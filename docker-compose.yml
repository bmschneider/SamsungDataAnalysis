version: '3'

services:
  jupyterlab:
    build: .
    container_name: sda
    volumes:
      - ./:/project
    ports:
      - "8888:8888"
    environment:
      - JUPYTER_ENABLE_LAB=1
    command: start-notebook.sh
